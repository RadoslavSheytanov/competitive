function getPersonInfoString(input) {
  let firstName = input[0];
  let a = input[1];
  let b = 3;
  let product = a * b;

  let result = `The architect ${firstName} will need ${product} hours to complete ${a} project/s.`;

  console.log(result);
}
